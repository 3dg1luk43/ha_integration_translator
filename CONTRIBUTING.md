# Contributing

## Local Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Validation

```bash
python3 -m py_compile translate.py
```

## Pull Requests

- Keep changes focused and small
- Include a clear rationale in commit messages
- Verify script execution on at least one integration translation folder
